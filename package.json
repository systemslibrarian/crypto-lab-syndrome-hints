{
  "name": "crypto-lab-syndrome-hints",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Runs a real information-set-decoding attack on an F_2 syndrome-decoding instance and interpolates its work factor from exponential (no hints) toward polynomial (enough hints) as side-channel hints accumulate — Hint-ISD, IACR ePrint 2026/341. A teaching demo, not production crypto.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:a11y": "playwright test"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.62.1",
    "typescript": "^7.0.2",
    "vite": "^8.2.1",
    "vitest": "^4.1.10"
  },
  "allowScripts": {
    "esbuild@0.21.5": true
  }
}
